name: route_performance_tracker
publish_to: 'none'
description: Local-first MVP for delivery route performance tracking.
version: 0.5.1+6

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  image_picker: ^1.2.3
  google_mlkit_text_recognition: ^0.17.1
  path_provider: ^2.1.5
  path: ^1.9.1
  http: ^1.5.0
  image: ^4.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
